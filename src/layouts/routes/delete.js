/*import axios from 'axios';

export const deleteResource = async (resourceType, resourceId) => {
  try {
    const response = await axios.delete(`/api/${resourceType}/${resourceId}`);
    return response.data;
  } catch (error) {
    console.error(error);
  }
};*/